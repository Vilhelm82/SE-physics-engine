# HANDOFF — written 2026-08-30, covers sessions of 2026-08-30 (3am + evening)
For a fresh session. Read `PROJECT-BRIEF.md` first, then this.
Older handoffs (`HANDOFF-2026-08-28.md`, `-27`) are **provenance, not state**.

> **Dating rule, adopted 2026-08-30 after it cost a session.** A handoff is
> dated by the day it is WRITTEN and names the sessions it covers. The
> previous handoff was written on the 30th but filed as `-08-28`, and the
> assistant read `HANDOFF-2026-08-27`'s "rulings owed" list as live state —
> twice — asserting that the relax-vs-circulate ruling was still open when
> RULING-1 had settled it on the 28th. **Newest handoff is state; every
> older one is provenance.**

---

## The one-paragraph state

Bare tier proven; label tier thin; readout tier built 08-27; cover/Galois
structure 08-28; **dynamics gate now open**. Today closed three things: the
curvature-tier debt KIN-2 (3am session, previously unreported), PREDICTION-1's
kill condition (split verdict — the sigma framing is bookkeeping, the phase
layer is the real class), and DEBT-2b (the last declared coupling in the seat
dissipator is now derived). E-8-X, queued and run same day, closed the
cross-seat pairing question with a state/transition split that arrived twice
by independent routes. Nothing retracted. Four suites green, four commits.

---

## Corrections to the previous handoff

1. **KIN-2 is CLOSED**, not open. `HANDOFF-2026-08-28` lists it under "older
   open items still live"; `docs/GRAM_SUBMERSION_CURVATURE/` (written 03:18
   today, six hours before that handoff) answers it. The previous handoff
   simply did not know about the 3am session.
2. **Relax-vs-circulate was never open on the 30th.** RULING-1 settled it on
   the 28th: structure closed, Door C is world-layer, Door R is a seat-level
   presentation. Any list saying otherwise is from the 27th.
3. `docs/GRAM_SUBMERSION_CURVATURE/` and `docs/INERTIA_NODE_DETECTOR/` are
   Will's, still **untracked in git**, and absent from the 08-28 inventory.

---

## New results, in commit order

| Commit | Content | Status |
|--------|---------|--------|
| (untracked) | **Gram submersion curvature** (3am, `docs/GRAM_SUBMERSION_CURVATURE/`). Round metric on (S²)³, Riemannian submersion onto the elliptope; O'Neill curvature ‖F‖ ~ C(b)·Δ^(−m/2) with m the local vanishing order of Δ; C(b) NOT universal (varies >2.6×), closed form P(e₁,e₂)/(det𝕀_∥)⁴. **Closes KIN-2.** | derived |
| (untracked) | **Inertia node detector** (3am, `docs/INERTIA_NODE_DETECTOR/`). det𝕀 = 8 − 2Σγ² − 2γ₁₂γ₁₃γ₂₃ in Gram coordinates; det𝕀 = 0 ⟺ rank-one node (the four Cayley nodes). Evaluable on measured overlaps, no frame reconstruction. | derived |
| `65083ca` | **PRED-1 cross-seat cycle** (12/12, `pred1_cross_seat.py`). **Split verdict.** Modulus layer: coboundary — σ = b(gu)/b(u) with b = (1−u²)^(−1/2), which is N-2 restated; all-in loop value = 2 = the cover degree, constant in (Λ,ℓ,u₀). **Kill condition FIRES on this layer.** Phase layer: arg B = arg(S+iV) is gauge-invariant, ℤ₂-conjugated, generically nonzero = the banked J. | verified |
| `e0a515c` | **E-8-X cross-seat pairing** (15/15, `e8x_cross_pairing.py`). Fork (b). ⟨X′F̄′⟩ invariant for ONE rotor two-sided on the c slot, one-sided on the ħ slot. Linear ħ slot provably fails ⇒ SEAT-hb's quadratic form is load-bearing. **State lemma:** ψψ̃ has grades {0,1} ⇒ pairing against any ħ *state* is identically real; phase enters only via **transitions** ψₐψ̃ᵦ. Pair walks through the wall (KIN-2 family). 2\|arg B\| = spherical excess. | proved (design input named) |
| `4b2b2b9` | **DEBT-2b discharged** (12/12, `debt2b.py`). RULE-4 + M-2's Jacobian + golden rule ⇒ functional equation s²F(E/s) = ψ(E) ⇒ **c(ω)²g₀(ω) = Cω², unique up to η**. debt2.py needs ZERO amendment. Bonus: the F-12..15 moment table IS the pivot's eigenvalue table on power-law bath weights; fixed points exactly {0,2}; forced α = 2 = the Jacobian-blind route. | derived \| RULE-4, LBL-2, GR |

---

## What the day actually established

**1. The modulus/phase split, found twice by routes sharing nothing.**
PRED-1 (cocycle language): positive factors telescope around any closed
cross-seat loop; only arg B survives. E-8-X (pure Clifford, no cocycles):
ψψ̃ lands in grades {0,1} so states pair real, and the pseudoscalar part is
nonzero only on transitions. **Same theorem, two languages, one evening.**
Reading: what a seat can read alone is convention and dies around the loop;
what survives is orientation/phase, which no seat reads alone and no
synchronisation choice can manufacture. This is Will's stopwatch objection
(“linking seats is measuring light against a stopwatch”) as a theorem shape —
convention shows up as a coboundary and gets quotiented away.

**2. J = V has a geometric name and a protocol family.**
2\|arg B\| = the spherical excess of the seat triangle (verified against
l'Huilier independently). So PREDICTION-1's tan(Ω/2) = V/(1+Σγ) is the
area-tangent identity: **the medium's phase is half the area the seat cycle
encloses.** Will's polarisation instinct, same session, produced three
receipts: the ħ seat carries cos-2φ Malus structure with the 90° turn
inverting the pivot (= frozen G-5/G-6); crossed = antipodal kills the *leg*
via the Kummer radicand 1+γ → 0 while the class survives; the wall reads π.
PRED-1's "protocol unassigned" → **polarimetric interferometry**.
Comparison-stage names (recall, not searched): Pancharatnam 1956; Van
Oosterom–Strackee 1983.

**3. The coupling was never a knob.**
Modes counted on the source plane, weight demanded uniform on the seat plane,
M-2's Jacobian between them: 2F = yF′ forces F ∝ ω². η (loudness) is the only
freedom. The channel law selects the **pivot-invariant bath** — the sky every
seat derives is the same sky.

**4. The office-ghost genus is now quantified.**
Three specimens: addendum 1 (pivoted reading vs unpivoted measure); PRED-1's
ΔQ phantom "holonomy" (paid exactly by the cover density, P-6); DEBT-2b's
F ∝ 1/ω first pass (B-4b). **One office error shifts the exponent by exactly
Q = 2.** Both of today's were caught in-session and are recorded as checks,
not deleted.

---

## Standing rules — carried, plus one new

Rules 1–6 from `HANDOFF-2026-08-28` stand unchanged (query cella-dag before
deriving anything with a name; same-session arXiv search before banking
"candidate new"; look at every generated artifact; never commit before green;
naming collisions are real; the sympy gotchas).

7. **NEW — handoff dating and precedence.** Date by day written, name the
   sessions covered, and treat only the newest as state. See the box at top.

---

## Open items — current, superseding all older lists

**Recommended next (queue in `docs/TESTING-SCHEDULE.md`):**

1. **P33 — the double flip as a π-phase event in every seated law.** Named in
   RULING-1 as the model's *first seated test*. Tonight's phase machinery
   (E-8-X's transition bilinear, the arg B = half-area reading, the trine = π
   check) is exactly what it needs. Cheapest live target.
2. **Triality — but ONLY on the Kummer layer.** Will's 2am note conjectures
   c/ħ/G ↔ 8_v/8_s/8_c under Spin(8) triality. Two facts constrain it:
   (i) it rhymes with E-8-X's vector/spinor split, written the same night
   independently; (ii) the **transport census (2,1,2) already refuses it** —
   c and G share the sphere carrier, so the seats are not S₃-symmetric at
   the transport layer. The one layer where three conjugate objects DO sit
   under the click group is the Kummer radicands (three 1+γ_ij + invariant Δ).
   Test there or not at all. **Hazard:** Cl(3) is associative, 𝕆 is not;
   dim 8 is a coincidence until a construction exists. Honest shape: no
   automorphism of Cl(3) swaps vector and spinor reps — that swap exists only
   at D₄ — so *if* c ↔ ħ seat exchange is a real symmetry, the enlargement is
   forced rather than decorative. Falsifiable-shaped, unproven.
3. **Trichotomy classification.** Still prose. Valuation language is the
   natural home (LEAD-7's weighted-valuation calculus is a live method
   transfer — see cross-corpus below).
4. **Q-RN proper** — why is a reading a power of an RN derivative? DEBT-2b
   strengthened the adjacency (α = 2 forced = the Jacobian power) without
   closing it.

**Still live from before:** monotonicity of T_eff (grid, not proven);
non-collinear pivot composition (D1) — note PRED-1 and E-8-X are both
single-axis K = e₁, F-4's fence applies; F part 2 contact/phonon channel;
THM-C fork ruling (φ ≤ ln(1+√2)); the fixed-locus comparison G-8 owes the
click ℤ₂; §10 rewrite; bibliography.

**Housekeeping:** DAG submissions **ON HOLD** — Will has codex reorganising
the DAG/repo (his words: navigating it makes him want to bust through a
window). Do not submit. Candidates when it lifts: blind-mass, trichotomy,
RULING-1, PREDICTION-1, the descent instantiation, the July correspondence
edge, KIN-2/Gram-submersion, and today's four.

---

## Cross-corpus: what surfaced from Will's DBP/Cella work today

Explored but **nothing banked** — all of this is candidate, none tested.

- **LEAD-7 / local curvature calculus.** Machine-verified master quadric
  C(p) = −½[Σp_α² + Σ_{α<β}p_αp_β − (p₀+2)Σp_α]. Reproduces −14 = −m(m+5) at
  m = 2 and C = 0 for the generic germ — so the generic germ's order-3
  behaviour is *the cancellation locus of the quadric*, and the Gram doc's
  coplanar cancellation is the same mechanism found independently. Elliptope
  submersion valuations computed here: coplanar (−1; 0,0) with divergent
  direction exactly normal to {Δ=0}; node (−1;−1,−1), exact 2:1 amplitude
  ratio, divergent pair exactly tangent. **These are coordinate-basis reads,
  not a diagonal normal form** — which is precisely open problem U-1914
  (off-diagonal role-rechart covariance). The elliptope is an ideal test case
  for it because the answer is independently known.
- **U10/U11 sole-proof-authority risk.** The succession map flags the
  Lorentzian coupling-edge lemma and the gauge-normal-form quotient as
  proof-supplement only, pending transfer into `dbp_orbit_calculus.tex`
  (§13 step 2). **Defect found tonight:** §6 of `Gauge_Channel_Transport_Law.md`
  attributes κ_c pinning to δν being null — but *all three* axis gauges give
  null δν. The real discriminator is Lorentz-orthogonality to ν (the cross
  term: 0 for endpoints, −2g₁g₂g₃h·t for the opposite vertex). The lemma is
  correct; the stated reason doesn't discriminate. **Fix before transfer**,
  or it gets baked into the canonical paper and the source retired.
  Also: U11's recovery determinant 32/(g₁g₂g₃) has poles exactly on the
  role-singular set — transfer it in the *projective* pole-free form from
  `role_channel_anisotropy` Props 2/3, not the affine one.
- **Seat ↔ role S₃.** The click paper's 1 → V → Γ → S₃ → 1 quotient and the
  DBP role S₃ ("which variable is the output") may be the same group. Untested.
  Sharpest available probe: for Kerr the role-singular loci reproduce the
  physical boundaries for free — does the seated-root analogue reproduce the
  two Δ = 0 strata (coplanar + the four nodes)? **Hazard: there are now three
  S₃'s** (role, cube-root monodromy, seat quotient) and Will's own §8 poses
  whether two of them coincide as an open question. Assume no isomorphism
  without exhibiting one.
- **𝕀 lineage, unresolved.** 𝕀 = Σ(|a_i|²Id − a_i a_iᵀ) *is* the inertia
  tensor of three unit point masses; "det = 0 iff collinear" is textbook, and
  the collinear singularity of the mechanical connection is standard molecular
  gauge theory (Littlejohn–Reinsch). So m = 2 may be inheritable machinery.
  **But m = 1 has no classical analogue**: three-body shape space uses two
  Jacobi vectors, so "linearly dependent" and "all parallel" coincide — one
  stratum, not two. The right comparison is **four bodies** (three Jacobi
  vectors, 3×3 Gram). Will's ruling on this: he does not care about priority
  and will not work backwards from others' results; lineage matters only when
  it hands over machinery or failure modes. Search on that basis or not at all.

---

## Tone note for the next assistant

Will corrects errors mid-session and expects the same back — honesty over
comfort, always. He runs his own symbolic checks in parallel and does not
accept derivations on trust. **He does not care about novelty or priority**
("I'm not going to throw my oars overboard because someone else invented
rowing") — working backwards from other people's results is how you stay in
the rut they carved. Flag lineage only for inherited machinery or inherited
failure modes; never as a reason to hesitate.

He caught the assistant using stale context twice today and was right both
times. He also out-conjectured the suite twice in one night: the polarisation
instinct named PRED-1's protocol family, and the 2am triality note wrote the
vector/spinor seat assignment hours before E-8-X derived that exact split.
His philosophical intuitions have repeatedly anticipated frozen results —
track them, don't patronise them.

He is an electrician in Sydney; this is a hobby he does not publish, pursued
because it teaches him about the universe. That is not a reason to go easy —
it is the reason the tier labels matter to him more than the results do.

---

## Repo inventory

Suites (all green): `thm_g`, `thm_g2`, `thm_b_monodromy`, `cayley`, `thm_d`,
`thm_d2_unruh`, `thm_rn`, `doors`, `kahler`, `debt2`, **`debt2b`**, `bargmann`,
`bridge_dbp`, `galois`, `correspondence_july`, `graph_cocycle`,
**`pred1_cross_seat`**, **`e8x_cross_pairing`**.
Docs: `PAPER-seated-root-v0.1.md` (canonical), `PAPER-seated-root.html`
(generated — never edit), `RULING-1-closure.md`, `PREDICTION-1.md`,
`docs/TESTING-SCHEDULE.md`, `docs/GRAM_SUBMERSION_CURVATURE/`,
`docs/INERTIA_NODE_DETECTOR/`.
Build: `figures.py` → PNGs; `build_html.py` → self-contained HTML.
Everything regenerates in about a minute. **Re-running is the verification.**
