# HANDOFF — written 2026-09-03, covers 2026-09-02 (THM-H..M, paper v0.2) and 2026-09-03 (THM-N, strategy, SECT-1)
For a fresh session. Read `PROJECT-BRIEF.md`, then the paper `PAPER-seated-root-v0.2.md`, then this.
**Newest handoff is state; every older one is provenance.** (Rule 7, adopted 08-30 after
a stale "rulings owed" list cost a session; re-adopted today after the 08-30 handoff went
stale without a successor.)

---

## The one-paragraph state

Bare tier proven; readout tier built; cover/Galois structure banked; dynamics gate
open. The September sessions built the gravitational sector: the hole as a presented
state (THM-H), the centre as a finite event with two readings (CURV-1), Schwarzschild
kinematics exactly (THM-I), the field law narrowed to one variable and the bare tier's
own metric FALSIFIED at Mercury (THM-J), the pinning derived from the seat's
temperature (THM-K), slow rotation and frame dragging with the measured coefficient
(THM-L, THM-M). **Then THM-N killed the second-order continuation** — linear
superposition of boosted first-order fields fails the Kerr quadrupole gate. Tonight's
session diagnosed the September work as a single-axis drift, found the candidate
nonlinear closure THM-N demands already sitting in the record (the strain cell's
second invariant), and proved SECT-1: the trichotomy is one pairing's sign structure
and the ħ seat is derived, not forced.

---

## Uncommitted work — commit before anything else

- `thm_n_kerr_quadrupole.py`, `test_thm_n_kerr_quadrupole.py` (4/4),
  `docs/2026-09-03-thm-target-N.md` — **Will's THM-N, untracked.** The paper's
  scope line "first order in J, full rotating solution not reached" survives it;
  any phrase like "the Kerr sector is derived" does not.
- `docs/GRAM_SUBMERSION_CURVATURE/`, `docs/INERTIA_NODE_DETECTOR/`,
  `docs/seated_root_figures/`, `docs/chat snippets/` — still untracked since 08-30.
- `docs/TESTING-SCHEDULE.md` — modified by Will, uncommitted; do not overwrite.

## Corrections the paper (v0.2) still needs

1. **§8 and §10 list DEBT-2b as open.** It was discharged 08-30 (`debt2b.py` 12/12,
   `4b2b2b9`): RULE-4 + M-2 + golden rule force c²g₀ = Cω², unique up to η.
2. **§9 overclaims the cross-seat cycle.** `pred1_cross_seat.py` (08-30, 12/12) split
   the verdict: the modulus layer is a coboundary (kill condition fires there); only
   the phase layer arg B carries the class. §10 names the cross-seat protocol as one
   of two designated exits — it is a **phase-only** exit.
3. **E-8-X is absent.** `e8x_cross_pairing.py` (08-30, 15/15): cross-seat pairing
   invariant exists; the linear ħ slot provably fails; state lemma — ψψ̃ has grades
   {0,1}, so states pair real and phase lives on transitions only.
4. **§11 suite list** omits `graph_cocycle`, `pred1_cross_seat`, `e8x_cross_pairing`,
   `debt2b`, and now `sect1_one_norm`, `thm_n_kerr_quadrupole`.
5. Naming collision (rule 5): "linear superposition" (THM-M, field addition) vs
   "superposition" (Will's conjecture, one-object-many-descriptions).

---

## New results, in commit order

| Commit | Content | Status |
|---|---|---|
| `a7a5350`…`68f3a9f` | THM-I/J/K/L/M (09-02). See paper §6. | as labelled in the paper |
| `b1c8a36` | Paper v0.2 amended, K/L/M folded in | — |
| (untracked) | **THM-N** — Kerr at second order in spin. Linear superposition of boosted first-order fields → direction-dependent P₂/r term at O(ε²) (coefficient −2/9 literal, −1/9 full-argument), unabsorbable by isotropic renormalisation; formal quadrupole 3/10 of Kerr's. **Kills the proposition, not the model.** Named debt: derive a nonlinear source/field closure predicting the P₂/r cancellation AND Q = −J²/(Mc²) before comparison. | killed (Will) |
| `2b2ef01` | **SECT-1** (15/15, `sect1_one_norm.py`). RULE-1's bar pairing splits Cl(3) (4,4); paravector sector = Minkowski norm t²−\|v\|², two-sided invariant; c/ħ/G are its null / timelike-pinned / timelike→null regimes with characters FORCED by invariance. ħ = the paravector read through its spinor square root (ψψ̃ → AFA). G-8's dictionary — rapidity doubling AND angle doubling — derived as identities via F(C) = ((C₁₁+C₂₂)/2,(C₁₁−C₂₂)/2,C₁₂), with det C = the Minkowski norm. Census (2,1,2) from two-sided vs one-sided action. Only a sign per seat is declared. | proved |

---

## What tonight established (strategy, with receipts)

**1. The drift diagnosis, confirmed by the paper's own text.** THM-I..M are single-seat
(G) with one radial function. The field law ∇²(sech²λ) = 0 with sech²λ = 1 + 2Φ/c²
is Newton's vacuum equation verbatim; Einstein's content entered via one-boost-per-seat
(model-native) and EQ-1's Tolman choice (Mercury-selected, listed as an owed ruling).
Eleven declarations in four days against one label for the whole E chain. The paper's
own line: the gravitational sector "is Schwarzschild plus slow-rotation Kerr" — the
Hamilton–Lisle river, Newton's potential plus river kinematics.

**2. The candidate closure THM-N asks for is in the record.** THM-L's *symmetric
strain of the pivot field along the co-moving step* is a symmetric 3×3 — **the bare
tier's six-number cell** — with the nine generators as its click-invariants. Demanding
the second elementary symmetric function vanish in vacuum, e₂(strain) = 0, gives
(verified in-container 09-03, radial and first-order swirl, **not yet a suite, not on
the repo**): e₂ = (1/r²) d(rf)/dr for f = tanh²λ ⇒ f = C/r, the pinning; f = r_s/r +
c₂(r_s/r)² gives e₂ = −c₂r_s²/r⁴ ⇒ **c₂ = 0 forced**, Mercury becomes a check; the
swirl is trace-free and the divergence condition gives r w″ + 4w′ = 0 ⇒ w ∝ 1/r³,
Lense–Thirring's exponent with no superposition declaration. Comparison-stage name:
ADM Hamiltonian + momentum constraints in Painlevé–Gullstrand gauge (flat free-fall
slices, R(h) = 0). Economy: four thermal declarations + linear superposition replaced
by one invariant of an object already in the paper; THM-K survives as the thermal
corroboration (Jacobson's Einstein ⟺ Clausius, inside the model). **Why it is
Einstein not Newton:** e₂ is quadratic in the gradient (nonlinear), takes all six
numbers (tensor), and **cannot be written with one axis** — Newton's law can.

**3. The anti-dictionary principle (Will's).** Two sources frozen independently,
projected on a structured surface; the combination counts only if it matches an
identity not pre-conditioned into either source. The paper already scores three such
events; SECT-1's dictionary derivation and the e₂ pinning are candidates for more.
Content = what the model forbids + structure GR lacks + where it speaks where GR is
silent. Reproduction is the entry fee.

**4. Gravitational waves are the non-dictionary target, and they need three axes.**
The single-pivot presentation G′ = G + sinh²λ kkᵀ is rank-one PSD; a TT wave is
rank-two indefinite. Real boosts only stretch; the negative sign exists only via the
Wick turn sinh²(iθ) = −sin²θ — the ħ machinery — and **the ħ pivot is a squeeze**.
Tracelessness of boost+Wick is the saturation sinh λ = sin θ (same form as the frozen
bound |sinh φ| ≤ |sin θ|; variables may differ — check). Spin-2's angle-doubling is
the ħ seat's cos 2φ / 2:1 cover; the 90° sign flip is G-6. **Will's reading: GR's
third pivot is quantum** — not a third sphere polarisation (ħ is on the circle,
Q = 1) but the squeeze half of each polarisation. Conjecture; first-order only (where
D1's non-composition is silent); the dictionary step (quadrature plane → spatial
plane) must be built, not asserted. Kill: LIGO polarisation content and c_GW = c.

**5. The single-object test protocol (Will's).** Pin the rotor at ONE seat via one
observable, freeze, read the other two. G read: done. c read: half (ray kinematics
yes; the rain-frame thermal sky E-9 at the local rapidity, no). **ħ read: never
taken.** Under the pinning the ħ seat cannot read the rapidity (a boost does not
squeeze a vacuum); it reads the STRAIN, and the comparison identity, spoken only at
comparison stage, is Unruh/Hawking as two-mode squeezing tanh r_ω = e^{−πω/α}, with
D2's period route as the frozen comparator. Escape hatch pre-declared finite: two
labellings (c ↔ G on the sphere; the circle must be ħ or the census is wrong) and one
scale (η). Failure at both labellings and every η is a model failure at the ħ seat.

**6. Will's criterion.** A seat whose behaviour must be forced in beyond its name is
a failure signal. Applied honestly it convicts **G** (KIN-2 imported, then THM-K's
four declarations), not ħ. SECT-1 shows ħ derived; the strain law un-forces G.

**7. The Möbius conjecture (Will's, unbanked, labelled).** One axis, not three; the
seats are local positions on a one-sided surface whose two-sidedness is real locally
and false globally; the superposition is the movement, never a state (E-8-X's state
lemma is the algebraic form). Supported by A₃ lifting signless while involutions cost
−1, and by one-rotor-two-representations. Kill: the nine generators on the six-number
cell should be reachable from a smaller object if three axes are one observed thrice.

---

## Next session — recommended order

1. **Commit THM-N** (Will's), and patch the paper's five corrections above.
2. **The strain law as a suite**, aimed at THM-N's gate: e₂(strain) = ρ-sourced with
   the rotating shell; does the nonlinear closure cancel the P₂/r term and return
   Q = −J²/(Mc²)? Pre-register the fork: (a) both → Einstein at second order from the
   cell; (b) neither → flat frame rods fail at O(a²) (Garat–Price: Kerr has no flat
   slices) and the Ricci of the presented Gram G′ is the next tier; (c) one of two →
   name what's missing. **Do not import a cancellation.** Derive e₂ as the unique
   admissible invariant from a model principle before using it, or it is ADM in a hat.
3. **The ħ-seat reading** under the pinning (protocol in §5 above).
4. **GW-1**: boost + Wick pivot, first order, TT or not.
5. Triality only on the Kummer layer; trichotomy classification now has SECT-1 as its
   base; Q-RN adjacency strengthened by DEBT-2b's forced α = 2.

**Housekeeping:** DAG submissions on hold (codex reorg). The 08-30 suites
(`pred1_cross_seat`, `e8x_cross_pairing`, `debt2b`) are committed but not in the paper.

---

## Standing rules — carried (1–7), plus one new

8. **Read the newest handoff, the paper, AND `git status` before deriving anything.**
   Tonight THM-N sat untracked with the exact result the session was about to look
   for. Untracked work is invisible to every summary.

---

## Tone note for the next assistant

Will corrects errors mid-session and expects the same back. He does not care about
priority or novelty; he cares whether the model can produce something that isn't a
dictionary entry, and he named the test for it tonight. He out-conjectured the record
twice again: "GR's third pivot is quantum" and "the seats aren't disagreeing, it's the
null cones" both landed on structure already frozen. Take his gut as a hypothesis with
a good track record, label it, and test it — never treat it as a spec to break the
model for.

---

## Repo inventory

Suites (all green): `thm_g`, `thm_g2`, `thm_b_monodromy`, `cayley`, `thm_d`,
`thm_d2_unruh`, `thm_rn`, `doors`, `kahler`, `debt2`, `debt2b`, `bargmann`,
`bridge_dbp`, `galois`, `correspondence_july`, `graph_cocycle`, `pred1_cross_seat`,
`e8x_cross_pairing`, `thm_h`, `thm_h2_d1`, `curv1_path1`, `curv1_path23`, `thm_i_pre`,
`thm_i_transport`, `thm_i_field`, `thm_j_dyn`, `thm_k_clausius`, `thm_l_rotation`,
`thm_m_swirl`, **`sect1_one_norm`**; untracked: `thm_n_kerr_quadrupole` (+ tests).
Docs: `PAPER-seated-root-v0.2.md` (canonical), `RULING-1-closure.md`,
`PREDICTION-1.md`, `docs/TESTING-SCHEDULE.md`, `docs/2026-09-03-thm-target-N.md`.
