# HANDOFF 2026-09-04 — the strain law, the pivot circle, the deck, and the arrow that is still owed

Read this before `git status`, then read `git status` before deriving anything (rule from 09-03; it paid again).
Supersedes `HANDOFF-2026-09-03.md` for state; that file stays for the K–N history.

## Repo state at handoff
- Branch `master`, HEAD `87f8ab3`.  Nine commits this session, in order:
  `e3d2851` THM-N (Will; was untracked, committed first) · `714bf5c` THM-O suite 33/33 · `e84f053` THM-O report ·
  `1d627b0` THM-O scope tightened + Will's clarification doc · `bea8e33` Will's addendum (conjecture tier) ·
  `9561c5a` VIEW-1 incidence 12/12 · `dd48c0d` CENSUS-C 6/6 (killed) · `1e00336` CENSUS-C corrected 7/7 · `87f8ab3` deck descent 10/10.
- **Uncommitted, Will's:** `docs/TESTING-SCHEDULE.md` (Will's earlier edits + my `0g` THM-O entry appended by edit, not overwritten).
  Commit it yourself or say the word.
- **Untracked, Will's, untouched:** `docs/GRAM_SUBMERSION_CURVATURE/`, `docs/INERTIA_NODE_DETECTOR/`, `docs/chat snippets/`,
  `docs/seated_root_figures/`, `__pycache__/`.  Not inspected.  Check whether any of them already contains what the next target needs.
- **Paper v0.2 is stale:** it does not yet carry THM-O, VIEW-1, CENSUS-C, or the deck descent.  §6 gains THM-O; the scope line
  (first order in J) is unchanged — second order now has a theorem saying why.  Owed.
- New suites and logs on disk: `thm_o_strain_law.py` / `run_o.log` (33/33, 117 s), `view1_incidence.py` / `.log` (12/12),
  `census_c.py` / `.log` (7/7), `thm_galois_deck_descent.py` / `.log` (10/10).  Re-running is the verification.
- New docs: `2026-09-04-thm-target-O.md` (report), `2026-09-04-axis-planes-rods-clarification.md` and `...-ADDENDUM.md` (Will).

## Results, with tiers

### THM-O — the strain law is Gauss and Codazzi on the seat's flat rods (`thm_o_strain_law.py`, 33/33)
- O-1 **[proved identity]**: for the presentation −dt² + |dx − v dt|² (unit lapse, flat rods, stationary shift), G_nn = e₂(sym ∇v)
  identically for three arbitrary functions (theorema egregium, R(h) = 0), and G_ni = −½[curl curl v]_i (our K = −S convention).
  e₂ is not selected from nine generators; it is what flat rods present to Gauss.  The vacuum vanishing is **declared: NORMAL-1**
  (no source through the seat's normal).  Scope: these are the complete NORMAL projections; the tangential G_ij are computable from
  (h, v) in the stationary case (O-6d does it) but no model law owns their value.
- O-2 **[derived | NORMAL-1 + flat rods]**: e₂ = 0 ⇒ rβ² = C.  Second derivation of the pinning, disjoint from THM-K's thermal one.
  Exponent only — the coefficient C = 2GM/c² is MASS-1/K-6's.  c₂ = 0 forced.  e₁ = 0 (β ∝ r⁻²) and e₃ = 0 (β′ = 0) dead.
- O-3 **[proved (exponent)]**: Codazzi ⇒ rw″ + 4w′ = 0 ⇒ w = A/r³ (constant = rigid rotation at infinity, excluded).  The amplitude
  A = 2GJ/c² is still THM-M's 2 = 1 + 1; sourced Codazzi **[not run]**.  The O(ε) cross term in e₂ vanishes for any β, w; the drag's own
  Gauss content −(rw′)²sin²θ/4 is the second-order source, quadratic in J.
- O-4 **[proved]**: the O(ε²) correction is a gradient (axis regularity); polarised Gauss is 4∂²ᵣ + (2/r)∂ᵣ + Δ_S/r², NOT the Laplacian;
  exponents (ℓ+1)/2 and −ℓ/2, half of Laplace's; Newtonian clock multipoles coincide for ℓ = 0, 1 only.  **Flat rods carry a mass and a
  boost and no other multipole, for any source.**  No P₂r^{1/2} mode ⇒ THM-N's Gate 1 passed by structure.
- O-5 **[proved / derived | transport = geodesic (THM-I/L)]**: exterior ψ₂ = a₀√r − (A²/10√r_s)r^{−5/2} + [b₂/r + (A²/8√r_s)r^{−5/2}]P₂;
  particular constants forced (explicitly perturbed field leaves O(ε²) residual).  Clock: no P₂/r, no P₂/r³.  Orbital invariant
  ½(Ω₊²+Ω₋²)(R_c) = r_s/2R³ − √r_s a₀/2R³ − (5/4)√r_s b₂R^{−9/2} + 0·R⁻⁵ + (7/8)A²R⁻⁶.  Gate 2 NOT REACHABLE: no R⁻⁵ slot.
- O-6 **[proved]**: vacuum in all ten components at O(ε⁰) and — new — at O(ε¹) (flat rods + curl-free drag is exact vacuum).  At O(ε²)
  G_rr, G_θθ, G_φφ ≠ 0 for every (a₀, b₂); a₀ drops out exactly.  Corroboration under Einstein's tangential law only.
- O-7 numeric path (mpmath, nested FD, 40 digits): e₂ = O(ε⁴) with the forced constants, O(ε²) with them doubled; curl curl v = 0.
- C-1/2 comparison: Kerr, same invariant, M/R³ + (3J²/2M)R⁻⁵ + 6J²R⁻⁶.  **FORK (b)** as pre-registered.
- **Failure vs disagreement, sorted (report §):** Gate 1 was an internal failure of THM-N's construction (K-6 multipoles, MASS-1 scalar
  monopole).  The exponent theorem fails FLAT RODS (K-6 as scaffold; J₂'s a^{−7/2} as ground via the same operator on a static oblate
  source), not the model — THM-M's static rods are a sum of rank-ones.  The rods-bend conclusion rests on O-4e + K-6 + J₂, not on O-6d.
  The tangential sector is where the model is allowed to disagree: silence, not error.  Kerr's Q = −J²/M is not yet a legal comparison.
- **Gate 2 re-declared:** (i) hole — Q by regularity at the wall (THM-H at O(J²)), compared with Kerr; (ii) shell — Q by source, compared
  with GR's shell (de la Cruz–Israel 1968, Pfister–Braun 1985), not Kerr.  Neither arises on flat rods.
- **Band / edge / position / surplus** (the format for every reproduction from now on): band = ADM constraints in PG gauge, Doran river at
  O(J); edge = flat rods carry mass and boost and nothing else; position = one boost per seat; surplus = none yet.  Two-thirds of a
  special-case identification.  **Scaffold register** opened: NORMAL-1 (removal route: sourced Gauss from the blind-mass theorem);
  K-6 linearity (ground beneath it: J₂); drag amplitude (removal: sourced Codazzi); EQ-1 Tolman (ground: Mercury).  Ground, never
  removed: Mercury 3, GP-B 37.2, J₂ scaling.

### Will's ontology clarification and addendum (`docs/2026-09-04-axis-planes-rods-clarification*.md`) **[conjecture tier, not model inputs]**
- Fundamental planes never deform (δḡ_P = 0); "rods bend" is shorthand for "the presented rod metric is non-flat".  Flat rods h = δ is an
  observer-side presentation restriction.  The O(J²) residue q_ij is a presentation residue; the map (hidden root motion) ↦ q_ij is the
  exact open debt; the criterion is the curvature 2-form of the torsion-free connection, never dE ≠ 0 alone.
- One local root, one 360° pivot orbit θ ∈ S¹, three distinguished alignments: pivoting to c, ħ, G is not changing seats.  This is
  RULING-1 taken literally (θ is the dial; the totality is the orbit).  Midsagittal movement of the hidden representative inside the
  root's invisible fibre; three allocations, equivalence unproved.
- GR conjecture as a factorization claim: incompleteness needs X₁ ≠ X₂ with Π_c(X₁) = Π_c(X₂) and a model-owned O separating them.
  Candidate pair: the deck τ (V → −V), O = arg B.  Particle-shadow superposition strictly prediction-only, outcome fork A–E, ban list.
- Open question flagged, unresolved: the docs say "c-seat" for the seat GR governs; the record labels it G.  Under the pivot picture the
  letters are marks on a dial and matter less; still, say which.

### VIEW-1 incidence (`view1_incidence.py`, 12/12)
- **VIEW-1 [declared, pending derivation from the presentation duty]:** one pivot orbit presents all three constants.  Then the pivot
  circle is C = S² ∩ Aff(a_c, a_ħ, a_G), unique **[theorem | VIEW-1]**.  Every circle through the root is an admissible motion; incidence
  picks, not the census.
- **[proved, any three vectors]**: 2A·a_i = V identically (A the vector area); 4|A|² = 1ᵀadj(G)1; h = V/2A, h² = Δ/(1ᵀadj G 1); great circle
  iff V = 0; h ∼ √Δ at the branch locus.  Spin lift −exp(iπhσ·n̂) = exp(−iπ(1−h)σ·n̂); +I at h = 1, −I at h = 0: the deck is reached exactly
  on the branch locus.
- **[proved, whole cell — upgrade of Will's "one-session check"]:** the circumaxis of the sign class ε has DUAL-basis coordinates
  (V/2)(ε₂ε₃, ε₃ε₁, ε₁ε₂) = the Cayley node γ_ij = ε_iε_j.  "Axes ∝ Σε_i a_i" is true only at the trine.
- **Correction:** the cyclic order read from +n̂ is always counterclockwise (n̂ is the triangle's own normal) — no deck information.  The
  geometric twin of sgn arg B is the cyclic order read from OUTSIDE the sphere, i.e. sgn h = sgn V (200 frames, both lifts).

### CENSUS-C (`census_c.py`, 7/7) — **KILLED as stated**
- The record's only ħ-flavoured angle on the cell is arg B.  **[numeric, 300 frames; exact at the trine]**: 2 arg B = the solid angle of
  the GEODESIC TRIANGLE through the three alignments (Gauss–Bonnet from interior angles).  arg B is a half-angle of a solid angle — the
  doubled-angle (spinor-of-vector) structure is realised on the cell by B itself, no circle needed.
- The forced circle's cap 2π(1−h) is a different function: 2π(1−1/√3) vs π/2 at the trine; ratio 0.10–1.00 across the cell.
- What holds: cap(h) = triangle + three circular segments, each a function of one leg γ_ij and h (cos Δφ_ij = (γ_ij − h²)/(1−h²)), all ≥ 0,
  azimuths summing to 2π.  **All angles live in the √Δ class (G-9); the conjugate triple lives in the legs' MODULI (G-8).**  My first
  verdict said "conjugate triple on the segments" — wrong, corrected in `1e00336`.
- Parity under the deck (C-4): arg B and h pure odd, |B| even, cap mixed — the Θ / Θ² / T pattern of `galois_horizon_cover_v1_0`.
- Consequence: the circle and arg B are the SAME sector's objects (orientation); the census's 2:1 is the square class, not a locus.
  "The circle must be ħ or the census is wrong" resolves as neither.

### THM-GALOIS-DECK-DESCENT (`thm_galois_deck_descent.py`, 10/10) — the algebraic half of the deck test, closed
- Methods taken from the Cella corpus (tools only, comparison stage): valuation parity along divisors (Kummer criterion), successive-norm
  cover polynomial + irreducibility at a rational point (allk_monodromy_certificates / entropy_sum_resolvent, re-implemented in sympy).
- D-1 valuation matrix of (Δ, 1+γ₁₂, 1+γ₁₃, 1+γ₂₃) along (Δ = 0, 1+γ_ij = 0) is the identity ⇒ rank-4 square classes (2nd proof of G-4/5).
- D-2 degree-16 norm polynomial at γ = (1/3, 1/5, 1/7) irreducible over ℚ (3rd proof).  D-3 clicks fix Δ, permute legs; C₂⁴ over F,
  (C₂≀S₃)×C₂ over the symmetric base | U-1603.  D-4 min poly of B over F: 16x² − 8(1+Σγ)x + 2Π(1+γ_ij), discriminant −64Δ ⇒ [F(B):F] = 2.
- D-5 collision stratification: the four Cayley nodes are the rank-1 Grams, A₁ points, Δ multiplicity 2 ⇒ a loop around a node does NOT
  flip √Δ; at (1,1,1) no leg vanishes, at each other node exactly two legs vanish to order 1 and ramify.
- **The deck τ is the inertia of the smooth Cayley cubic and nothing else.**  Everything algebraic about sgn arg B is now said, three ways.

### ARROW-1 (Will's DBP lift, session end) — **REJECTED, not committed as a file**
- Three objections, in order of severity: (1) the self-glue dichotomy (pure power ⇒ C_m vs trinomial ⇒ S_m) is EMPTY at m = 2, and every
  cover in the seated root is a square root — solving Δ = 0 for an angle is quadratic (discriminant 4(1−γ₁₃²)(1−γ₂₃²), roots
  cos(θ₁₃ ± θ₂₃)); C₂ = S₂; C₂≀C₂ = D₄ with abelianisation (ℤ/2)², characters ±1 only.  (2) Monodromy characters are discrete roots of
  unity on the Gram cell; arg B is a continuous holonomy on the view sphere — different loops, different objects; the only monodromy touching
  arg B is the C₂ that flips its sign.  (3) The (c,G,ħ) ↦ (D,S,P) dictionary declares a new asymmetric constraint surface the record does
  not contain; the Cayley cubic is S₃-symmetric and SECT-1's regimes are values of one norm.  As prepared, ARROW-1 renames the sign.
- **Candidate arrow, mine, [hunch]:** the model owns one asymmetry — the G regime is FALLING (inflow; the sign of β is the black/white
  choice; the record picked inflow).  If THM-K's screen thermodynamics fixes sgn β, the falling regime carries a derived time arrow and the
  pivot orbit's sense can be read off it (with the river vs against).  **Kill, cheap:** flip the sign of β in the K suite; if every
  declaration and check survives, THM-K is time-symmetric, the model has no arrow, and sgn arg B is gauge for good.
- **Will says he has a solution.  Next session starts there.**  Whatever it is, it must supply an ORDERING OF THE TWO DECK SHEETS from a
  model principle — the horizon paper's S₊ > S₋ — not an algebraic property of the cover (D-1..D-5 exhaust those).

## Cella corpus locations inspected (comparison stage only; tools, not inputs)
- `~/Cella Framework/03_PUBLICATIONS/01_COMPLETED_PAPERS/galois_horizon_and_kummer_covers/`: `galois_horizon_cover_v1_0.tex` (the
  principle "invariants descend to the observables and odd data does not; the sign of Θ is the datum the Galois quotient forgets"; the
  ordering S₊ > S₋ is what makes the odd datum physical; extremal chirality = death of the odd sector at the branch point = R → −I at
  Δ = 0 here), `ALL_K_TWO_RADICAL_KUMMER_CLOSURE_v1.0.tex` (γ(γ−4P) = 4uβ² is the same shape as G-1's (1+Σγ)² + Δ = 2Π(1+γ_ij)),
  `self_glue_monodromy.tex` (the DBP asymmetry; needs m ≥ 3), `GENERIC_SYMMETRIC_MONODROMY_...md`, `kummer_closure_modules.py`.
- `~/Cella Framework/Papers_Library/07_certificates_data_and_reproducibility/` (the `07_CERTIFICATES...` entry under 03_PUBLICATIONS is a
  symlink to it): `galois_horizon_and_kummer_covers/` (allk_monodromy_certificates.py = Dedekind/cycle-type machine; entropy_sum_resolvent.py
  = exact norm resolvent; stage2/stage6 .m2 = valuation and collision tables); `local_curvature_and_black_hole_metrics/` (kerr_isco_pro_deep_sweep,
  kn_three_face_field, kerr_extremal_alpha_recovery, collision_field_read — for the O(J²) comparison and possibly CURV-1's collisions);
  `geometric_fault_localization_and_decomposition/` (rur_tools.py — the algebra for the circumaxis-node theorem over the cell).

## Rules learned this session (add to the standing set)
1. **Real runs on the workstation, always.**  The container is a 4 GB sandbox whose processes are reaped between calls; THM-O took forty
   minutes and three kills there and 115 s on the 7800X3D.  Container for syntax checks only.  Tee to a log the operator can `tail -f`.
2. **Desktop Commander `write_file` works** (chunks ≤ ~60 lines, mode rewrite/append) and `edit_block` works; the 09-03 hang note is
   stale for this session.  Do not route file transfers through downloads; write straight into the repo.
3. `sp.solve` on an inconsistent linear system can return a partial solution silently — use explicit linear algebra (solve at one point,
   check residuals at others).  `subs(A**2, ...)` inside a simplified expression can silently not fire — build perturbed objects explicitly.
   Declare `r` positive or sqrt simplification stalls.
4. **GR is never the arbiter.**  Failure = contradicts a model theorem, a model-owned measured number, or the model's axioms.
   Disagreement = a definite, integer-structured prediction where GR is unmeasured.  Assume the model is right and ask what breaks.
5. Every reproduction carries band / edge / position / surplus; a special-case identification needs the fourth.  Scaffold register per
   result: ground / declared / scaffold, with the removal route.  Removing scaffolding by renaming it is the trap; loud naming with the
   edge is the escape.
6. Parse compressed phrasing literally, then verify: "conjugate triple on the segments" was mine and wrong; "axes ∝ Σε_i a_i" was Will's
   and only true at the trine; "cyclic order from +n̂" carried no information.  All three caught by running, not by reading.
7. Monodromy characters (discrete, Gram cell) and Berry/Bargmann phase (continuous, view sphere) are different objects.  A theorem
   about one does not transfer to the other without a loop identification.

## Next session, in order
0. **Will's arrow.**  He has a candidate ordering of the deck sheets.  Take it through: (a) does it come from a model principle, not the
   cover's algebra; (b) does it survive the flip-β kill; (c) does it change under the click S₃ (it must not — Δ is click-fixed, D-3a).
1. **Flip-β kill on THM-K** (cheap): is the falling regime's direction derived or declared?  Decides whether the model has ANY arrow.
2. **Sourced Gauss from MASS-1** — NORMAL-1's removal route.  Sourced Codazzi for the drag amplitude (does 2 = 1 + 1 come out?).
3. **Tangential-law candidates on a STATIC OBLATE source** at O(ε⁰), before any spin: Δ/det G′ harmonic (THM-I), Tolman on a rotating
   screen (THM-K), integrability of rule G′.  Integer exterior multipoles or dead.  Survivors → O(J²) → κ_model → then Kerr is legal.
4. **BEND-1 / midsagittal projection law**: tilt residue q_ij = −sin²α t_it_j (rank one, contracting, the mirror of a boost); judged by the
   curvature 2-form only.  Second boost (PSD) vs squeeze (trace-free indefinite) is the pre-registered fork; shares the pivot with GW-1.
5. **Paper v0.2 update** with THM-O, VIEW-1, CENSUS-C, deck descent; scope line unchanged.  Commit the schedule.
6. ħ-seat reading (floor A₀² as a derived number) and the c/G labelling ruling, as queued.  DAG submissions still on hold.

## Late additions (same session, after the handoff was first written)
- `b97aa83` **PRED1-DECK-SEPARATOR** (`pred1_deck_separator.py`, 10/10): Will's resolution.  The deck test needs a SEPARATING OBSERVABLE,
  not a global sheet name; my S_+ > S_- success condition is withdrawn.  <Psi_A|U_(123)|Psi_A> = B_A exactly; Im = V/4; U_q^{-1} conjugates;
  O_q(tau A) = -O_q(A) under the same apparatus; click-equivariant; comparator identities exact.  PREMISE closed: the equatorial reflection
  leaves THM-M's full presented river (inflow + drag) invariant as a field, fixes the Gram, flips V -- Pi_G(tau X) = Pi_G(X), tau X != X.
  (My falling-regime arrow hunch is dead: a reflection fixing the inflow exists.)  Descent script corrected: B in F(i, sqrt Delta); the
  contradictory closing sentence withdrawn -- non-descent is the point.
- **PROTOCOL-1** (`pred1_protocol.py`, 11/11) -- the remaining debt, paid in the record's own objects.  Two directed protocols read V with the
  sense of the sequence: (K) the CLICK protocol -- the ordered product of the three idempotents (1+a)/2 has scalar part (1+sum gamma)/4 and
  pseudoscalar part V/4, reversion conjugates [proved, general axes]; (P) the PIVOT protocol -- carry the spinor root around the cycle by the
  three geodesic co-pivots R_{a->b} = (1 + b a)/|1 + b a| (E-8-X's rotor, two-sided on the axis, one-sided on the spinor); the axis returns,
  the spinor returns with phase conj(B)/|B| for temporal order 1->2->3 (200 frames; exact -pi/4 at the trine).  Same temporal order, same
  phase, in both protocols; PRED-1's B = Tr(P_1 P_2 P_3) is rightmost-first, i.e. temporal 3->2->1 -- a convention worth writing down.
  Lift-independent, click-equivariant, deck-odd.  Readout: pair the returned spinor with the kept one against a delay,
  I = 2 + 2 cos(delta + chi) -- the sign is read against LATER, never against LEFT.  Comparison: Pancharatnam (click), Berry (pivot),
  polarisation optics (Jones = spinor root, Stokes = axis, SECT-1 D-3 = the Stokes construction); classical intensities, no Born rule,
  no hbar -- the hbar SEAT contributes the one-sided representation, not a quantum of action.
- **Declare PROTOCOL-1:** cross-seat cycles are directed; a protocol is a temporal sequence of clicks or co-pivots; the sign of the
  seat-cycle phase is defined relative to the sequence and read against a delay.  The model's ordering of the deck sheets is an order of
  operations.  Ledger: [declared].  Removal route: none needed -- it names a fact about measurement, not about the state.
- PREDICTION-1 now has its protocol assigned.  Next on this line: the paper (PRED-1 section: protocol, sign convention, the trine number
  -pi/4), and the DAG entry when submissions resume.  The polarisation realisation is a bench experiment; nothing in it is new physics --
  the prediction is that the SAME phase is the orientation of three seat axes, which no single-seat presentation can read.

## RETRACTION OF STATUS (Will's ruling, end of session): PROTOCOL-1 is a WITNESS, not a derivation
- `pred1_protocol.py` was built with the answer known (Pancharatnam / Berry / polarisation) and selected to return it.  Its checks are
  exact; its SELECTION was steered.  The addendum's dependency -- existing model -> derived observation map -> unexpected structure ->
  interpretation, never in reverse -- was violated by Claire.  Status: **[existence witness, steered]**: the record's objects CAN be
  arranged to read V with the sense of a sequence.  NOT **[derived]**.  "PREDICTION-1 has its protocol assigned" is withdrawn.
- The derivation debt is REOPENED and CONTAMINATED: the answer is in the repo.  Remedy = the addendum's own ordering + preregistration:
  1. Derive the unrestricted pivoted observation map Pi_{s,theta} from one root, the pivot family, E-8-X's rotor and VIEW-1's circle --
     a runner whose SPEC never mentions cycles, clicks, phases, spinor holonomy, or this file.  Do not open pred1_protocol.py first.
  2. Search its OUTPUT afterwards for deck-odd terms (addendum s.25 E list).
  3. **Sealed kill (2026-09-04, before the runner exists):** if the derived map's output is deck-even, PREDICTION-1 is dead regardless of the
     witness.  **Claire's sealed prediction, held out of the spec:** the odd term, if present, is the pseudoscalar part of an ordered
     product of the seat's own idempotents.  Outcomes: (a) something else reads V -> that is the model's protocol, the witness was
     scaffolding; (b) the witness's form -> promoted to derived; (c) nothing -> the witness is a construction the model does not own.
- Same shape as THM-O's O-1 (the Hamiltonian constraint was known to be e_2 before it was written) -- but THM-O was labelled a band with
  an edge, not "the model produced a closure".  Rule: a construction steered by a known answer is a witness or a band; it is never
  "falls out for free".  Add to the standing set.

## PIVOT-MAP run (`pivot_map.py`, 6/6; spec frozen before output, verdict computed from the table) -- OUTCOME (a')
- Spec: one root; pivot axis = VIEW-1's circumaxis; pivot rotor two-sided on axes, one-sided on spinor roots (geodesic from the
  observer's own axis, no external base); ALL bilinear bar-pairings among {axes, transitions psi_i psi_j~}, S and I parts; mechanical
  search for pivot-invariance and deck parity under two realisations of tau.
- Found: every reading is pivot-invariant (E-8-X X-3/X-6 recovered); axis-axis = the Gram, no I-part (state lemma X-4 recovered);
  the map's output is NOT deck-even -- the sealed kill does not fire; the separating readings are exactly the transition pairings
  (axis-transition pseudoscalar = X-6c; transition-transition scalar), of MIXED parity; NO reading of definite odd parity exists; the
  relative-rotor 'cycle' is the identity (telescopes) -- 'cycle' is the wrong word for the record's transitions.
- Against the sealed prediction (b): FALSE.  The record admits bilinear pairings and Gram click-invariants (tr, e2, det of G' --
  orientation-blind), not ordered products of idempotents.  The witness pred1_protocol.py uses an operation the record has not
  declared.  It stays a witness.  Claire's first draft of pivot_map.py had a verdict written before the run; the run contradicted it;
  the verdict is now computed from the table.  Two reversals in one session on the same point: the rule is now load-bearing.
- The orientation reference the map does carry: the sign of the circumaxis n = A/|A|, i.e. the CYCLIC ORDER in which the three
  alignments are named (odd relabellings flip it).  Minimal PROTOCOL-1: naming (c, hbar, G) in an order is already the declaration.
- Owed on this line: closed forms of the mixed readings' odd parts (I-part/V ranges +-15 over 240 slots -- a Gram-and-h coefficient);
  a decision on whether the model declares a product rule for presented objects (which would admit the witness) or lives with the
  mixed readings; and, in either case, the physical protocol is now E-8-X X-6c read at the observer's own circumaxis, not a cycle.
