# Scaffold register for paper v0.6 -- what 2026-09-04/05 did to v0.5

Written 2026-09-05 evening (Claire) against HEAD `96825fe`. **Status: LEDGER.** Changes no number and no result. It relabels
tiers, records rulings, defines terms, and lists corrections, each with its receipt (commit or doc). It is the scaffold entry
both foundation audits asked for as their first recommendation. Per Will's 09-04 ruling the physics sections of the paper are
not touched; this register is what v0.6 cites when it is.

Tier words as used here: DECLARED (an input with its cargo), DERIVED (from the primitives), DERIVED | X (from the primitives
plus the named input X), RETRODICTION, CONJECTURE, HUNCH.

---

## 1. Ruling on the state space

**v0.5's bare tier is not the model's.** Sections 2 and 9, THM-H..O, CURV-1 and PRED-1 live on three unit vectors in S^2 with
a positive-semidefinite Gram: the elliptope, Cayley's nodal cubic, the click group S_4, the Bargmann phase, VIEW-1's circle.
The rebuilt model (T7 onward) has ONE form of signature (2,1) with ONE negative line (T7b1, `fe1a795`); no seat's resolution
is a positive-definite Gram. The two are the two real forms of one complex symmetroid: det G(a,b,gamma) = -Delta(ia, ib, gamma)
(Codex audit sec. 3.3; MATH-FOUNDATION-AUDIT sec. 4, `8771dda`).

**Birth certificate.** The Euclidean cell is not "Cl(3) residue." The ruler's own state space {q = 1} carries dS_2
(-du^2 + cosh^2 u dphi^2, depth timelike); its Wick rotation u -> iu is the round S^2 (KIN-1 k5e-k5f, `0886ea6`). (S^2)^3 is
the compact real form of the ruler manifold. That sentence replaces "residue" in v0.6.

**Migration table** (from MATH-FOUNDATION-AUDIT sec. 4, unchanged):
- survives verbatim: everything over Q(gamma) or C -- irreducibility of Delta, the square classes, the conjugate Kummer module,
  the descent B in F(i, sqrt Delta), the Z_2 monodromy and its node dichotomy (radicand becomes -det G = D^2 in seat coordinates);
- changes: the branch locus has no real nodes (T7c10); the solid angle becomes the hyperbolic area (T5 part A, `ae7c27a`); the
  spinor lift is Pin(2,1); the Bargmann phase becomes the block-resolved spinor trace V_spin = 2D (T5c, `749c9a7`);
- must be re-run on the (2,1) frame before v0.6 cites it: VIEW-1's circle, CENSUS-C, pivot_map, PRED-1's three protocols,
  CURV-1's centre readings, THM-H's null radii. None has been.

## 2. Three relabels (no number changes)

**R1. Signature (2,1): DECLARED, not reached.** P3 assigns a character to each plane and P11 resolves the frame into one
negative line and two positive ones; T1, T2, T7a and T7c11 are the classification of real forms of so(3,C) under that
declaration and could not have failed (both audits, sec. 2.1/3.1). v0.6 ledger line: *signature (2,1): declared by P3/P11
as written in `docs/results/2026-09-04/2026-09-04-PRIMITIVES-v0.md`; T1-T2, T7a consequences.* Kill: a runner producing (2,1) from primitives
that assign no plane character. T2's {0, 2} boost-plane count is scoped to diagonal realisations of an orthogonal frame
(Codex audit 3.1: G* with off-diagonals -2 spans three hyperbolic planes). T7a's "unique glue" is unique among BILINEAR forms
(Codex audit 3.2: L_epsilon has the same plane restrictions, parallelogram defect 2 epsilon/9); a polarization/linearity line
belongs in the primitives before they freeze -- the same line KIN-1 needs (k2/k5).

**R2. T8c: DERIVED | Maxwell duality compact.** "Duality on 2-forms is compact" and "n_- odd" are one statement in two
vocabularies, and Maxwell is not among the runner's listed inputs (audit sec. 2.6; Codex 3.11). Removal route, named and not
run: P5 gives s = +1 ("one clock"); declare "the arrival operation is an observable" as a model principle; T8b's identity
signature(Gamma_4) = (adjoint class) x s then gives Gamma_4^2 = +1 with Maxwell moved to the comparison stage.

**R3. T4f3: a CONSISTENCY of two declared conventions**, Bekenstein's 1/4 and the KMS circle, not a selection of 2pi
(audit sec. 2.4). The model-internal content of the period is: P4 makes the state angles between lines, a seat reads the
vector representation, and its period is 2pi. T4's header should carry the pinning's provenance (SCREEN-1, THERM-1, MASS-1,
EQ-1 via THM-K).

## 3. Terms v0.6 may use, with their definitions and receipts

**The pinch.** P_sigma = {gamma = sigma, a = sigma b}, sigma = +-1; codimension 2; seat coordinates {t = 0, l1 = l2} and
{t = pi, l1 = -l2}. It is the TANGENCY of the branch divisor {delta = 0} with the chart boundary {|gamma| = 1} of the cover
K(sqrt delta)/K -- grad delta parallel to grad eps along the whole line; delta = z1^2 (even, unramified) along the horizon and
order 1 (odd, ramified) across the branch curve (PINCH-3 c6, `33266bc`; U-1286 parity). The frame collapses to two lines there,
h = sigma g, and Will's null n0 shrinks to the zero vector (PINCH-1 p3, `67099dd`). Every base-field invariant is 0/0 there;
transversally the 0/0 is removable with a unique limit -- F -> sech^2 l, tilt = depth, and the seat's normal converges to
nu* = (-c + a h)/(1 + a^2), timelike (PINCH-1 p5); along the horizon it converges to one of two null vectors, one per sheet,
and nu* is their bisector (PINCH-1 p6-p7). Resolved by a weighted (1,2) blow-up whose exceptional divisor is coordinatised by
xi = (b - a gamma)/d = 2(b - a gamma)/V_spin (Codex `1526a4d`; PINCH-2 `bab32b7`); the deck sends xi -> -xi and glues the ends.
Codex's three-regime classification of approaches (overlap audit sec. 3, 14:09) precedes PINCH-1 (16:21); the balanced-face
weight (1,2) is the Newton polygon of delta (PINCH-3 c2). Dynamically, (xi, d) is the exact minimum predictive state at the
pinch and the two sheets give opposite signs of dF/dt under a fixed input (Codex `63d951e`).

**Seat-death and trapping.** Seat-death -- the seat's normal nu going null -- is native and soldering-independent. Trapping
is an area-transport statement. F_trap = F + 2 theta_K / rho; they coincide iff D = F Xi with 1 + 2 Xi_H/rho_H > 0 (Codex
rest-frame-trapping theorem, `1526a4d`; verified `d3c5465`). Seat-death occurs in Minkowski space for an admissible
soldering. v0.6 does not call {|gamma| = 1} "the horizon" without naming the area law that makes it one.

**The horizon-crossing chart.** From the dual normal nu = G^-1 l: [q] in (k, l, e_perp) = [[-F, 1, 0], [1, 0, 0], [0, 0, 1]],
det -1, regular through F = 0, with dF|_H = -2 sigma/(a - sigma b)^2 dgamma (Codex `1526a4d`; verified `bd98c17`). Requires
a declared soldering; excludes the pinch by hypothesis.

**The kinetic cost.** A FORK, declared at exactly one point -- which object the cost is quadratic on (ambient vector, or the
ruler's state space). Three positivisations of dS_2: ambient-Wick (Codex, K = -sech^2 2u), sign-flip (H^2), Wick (S^2).
diag(3, 1, 2/3) is DECLARED (KIN-1, `0886ea6`). Equal ruler weights are T7b3; the azimuth quotient is P4; kappa = 1 is pinned by
T1's conjugator S = diag(1,1,i). The load is the only closure.

**"Horizon = open circuit."** A HYPOTHESIS. The seat's algebra fixes eta = Z_s/Z_l = W/eps and nothing else; open and short
are indistinguishable to it; one absolute impedance (THM-K(a)'s Z) is missing (OC-1 o3, `313db70`). Under Codex's first
concrete load the horizon is not a constrained-output face at all (OC-1 header, `86f3ebc`). Passivity plus the sign change of
eps forces a fork: response ~ F^2 (passive interior) or ~ F (active interior -- the white-hole sheet in circuit language).

**hbar/G.** The distinction is the orientation of the frame, i.e. the sheet; the Gram cannot see it for the same reason it
cannot see the deck; the swap IS the deck on {a = b} and on the whole sigma = -1 horizon (HG-1, `96825fe`). Closes the 09-04
handoff item. Observed dynamically only.

**Cella's coupling form.** 2I - 11^T is the Gram of three unit spacelike lines pairwise at the horizon condition -- the
triple horizon, the 120-degree trine at depth asinh(1/sqrt3); its null cone is the three horizon light rays (XLINK-1,
`7c914f9`). CONDITIONAL on two obstructions recorded in its header: arbitrary gauge moves nu anywhere (Codex reconciliation
sec. 1), and ker Sigma is divisible so cannot generate the sheet (wall-isotropy). The nu-map is open and Codex's.

## 4. Corrections made 2026-09-05, all recorded in the file that made them

1. T7e e6b was a bare `True` (rule 12) and overclaimed "requires"; replaced by e6b (generic, computed) + e6b' (the pinch,
   computed) (`bab32b7`).
2. KIN-1 first run failed k3d: T7b3 fixes the weight ratio, not the overall scale (`0886ea6`).
3. PINCH-1 p3a/p3c, PINCH-2 q3c/q3d, PINCH-3 c5/c6, XLINK-1 x8c, HG-1 h6a: runner bugs (normalisation, placeholders,
   radicals, a scale, a double negation), each fixed with the claim unchanged and the failure kept in the header.
4. OC-1 o7: Claire conflated the kinetic block with the dissipative R; no tension with Codex's f^2 law (`313db70`).
5. PINCH-1 priority: Codex's three-regime classification at 14:09 precedes PINCH-1 at 16:21; Claire had declined to open the
   file under a "corpus enters only at comparison" rule Will never made; the duplication is that rule's cost (`35db9de`).
6. PINCH-3 c5c: Codex's corner-cancellation counterexample -- the value at the branch parabola needs the inward jet, which
   the runner does not compute (`35db9de`).
7. "Approach direction" (Claire) was wrong; the invariant is contact order, then the order-pair within it (PINCH-1 p9).
8. Codex's second audit "block exchange E commutes with two generators" is answered by Addendum 2 (Gamma_4 makes the grade
   involution inner; anticommutation is the job, not a demand); her "four signs" are mapped by T4/T5b/T5c with the block
   label declared as the reading. Both redundant, not wrong.

## 5. What v0.6 must not build on as reached

diag(3, 1, 2/3); "horizon = open circuit"; {|gamma| = 1} as a horizon without an area law; "sgn D constant along any regular
trajectory" (retracted 09-04, `17962af`); the coupling-form null cone as a statement about any gauge-fixed nu; T8c's
Gamma_4^2 = +1 without "| Maxwell"; the Euclidean cell as the bare tier.

## 6. Owed, and to whom

- The seat/load response, the nu-map, and the general theory of response on branched resolved state spaces: Codex, in
  progress (`e907a6f`, `4c47259`, `63d951e`).
- The primitives freeze, with the polarization/linearity line (R1) added first: Will.
- The horizon-as-boundary note (audit sec. 2.9: the seat's readout carrier is the boundary of its H^2; eta -> oo is the state
  going to that boundary; Hawking's period is the complexified boundary action): Claire.
- The Cella-side items outside this repo: the ROI outlook update and the groupoid equality fix (`788cf4a` in the Cella repo,
  applied 2026-09-05): Will.
